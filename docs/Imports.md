# Imports
You can import other procedures from outside the running directory of the root procedure. You can use the `import` keyword to do this.

Example:
```ipl
import "../module.ipl" as "procedureName"
```